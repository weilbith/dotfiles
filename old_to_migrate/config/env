# shellcheck disable=SC2034

MAIL_DIR=$HOME/mail
CONTACT_DIR=$HOME/contacts
CALENDAR_DIR=$HOME/calendars

MUTT_CACHE=$XDG_CACHE_HOME/mutt

ISYNC_CACHE_DIR=$XDG_CACHE_HOME/mbsync
ISYNC_STORE_DIR_LIST="$MAIL_DIR/private $MAIL_DIR/brainbot"

NOTMUCH_CONFIG=$XDG_CONFIG_HOME/notmuch/notmuchrc
NOTMUCH_DATABASE_DIR=$MAIL_DIR/.notmuch
NOTMUCH_HOOKS_DIR=$NOTMUCH_DATABASE_DIR/hooks

VDIRSYNCER_CACHE_DIR=$XDG_CACHE_HOME/vdirsyncer

GPG_TTY=$(tty) # Fix pinentry for gpg decryption

# vim: nospell filetype=sh
