- name: Install package
  aur: name=bashmount

  # For some reason trizen does complain here about root rights if given...
- name: Link configuration files
  file:
    src: "{{ role_path }}/files/"
    dest: "{{ configuration_directory }}"
    state: link
    force: yes
